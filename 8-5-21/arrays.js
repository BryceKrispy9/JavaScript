var generatedArray = new Array(3);

var generatedArray = new Array('Altuve', 'Correa', 'Spring');

var myArray = [1, 2, 3];

var myArray = ['Altuve', 'Correa', 'Spring'];

myArray[0]; // How to pick out a value from an Array // myArray[0][1] (If nested)

var playerName = myArray[1]; // Find what's at that index value

var mixedArray = ['Hi', 1, ['a', 'b', 'c'], { name: 'Kristine' }, function greeting() { console.log('Hey there');}] // Collection of data = Array - Used for database queries

mixedArray[4](); // Run function inside array